################################################
# !!!! DO NOT DELETE ANY FIELD OF THIS FILE !!!!
################################################

# Compiler name.
#   On Windows desktop, could be "vc140", "vc120", "auto".
#   On Windows store, not support.
#   On Windows phone, not support.
#   On Android, not support.
#   On Linux, could be "gcc", "auto".
#   On MacOSX, not support.
#   On iOS, not support.
compiler		= "auto"

# Toolset name.
#   On Windows desktop, could be "v120", "auto".
#   On Windows store, not support.
#   On Windows phone, not support.
#   On Android, not support.
#   On Linux, could be "auto".
#   On MacOSX, not support.
#   On iOS, not support.
toolset			= "auto"

# Target CPU architecture.
#   On Windows desktop, could be "x86", "x64".
#   On Windows store, not support.
#   On Windows phone, not support.
#   On Android, not support.
#   On Linux, could be "x86", "x64".
#   On MacOSX, not support.
#   On iOS, not support.
arch			= ("x64", )

# Configuration. Could be "Debug", "Release", "RelWithDebInfo", "MinSizeRel".
config			= ("Debug", "Release")

# Target platform for cross compiling. Could be "auto".
target			= "auto"
